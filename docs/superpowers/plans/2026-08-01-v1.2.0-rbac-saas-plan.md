# Implementation Plan: Milestone v1.2.0 (RBAC & SaaS Management Engine)

Branch: `feat/v1.2.0-rbac-and-saas-engine`

## Tasks

### Task 1: NestJS RolesGuard & @Roles Decorator (Issue #16)
- Create `apps/control-plane-api/src/auth/roles.decorator.ts`.
- Create `apps/control-plane-api/src/auth/roles.guard.ts`.
- Protect sensitive controllers with `@UseGuards(JwtAuthGuard, RolesGuard)`.
- Commit message: `sec(rbac): implement RolesGuard and @Roles decorator for NestJS endpoints`

### Task 2: Tenant Members Management CRUD API (Issue #17)
- Add `getMembers`, `addMember`, `updateMemberRole`, `removeMember` methods to `TenantsService`.
- Add endpoints `GET /v1/tenants/members`, `POST /v1/tenants/members`, `PATCH /v1/tenants/members/:userId/role`, `DELETE /v1/tenants/members/:userId` in `TenantsController`.
- Commit message: `feat(api): implement tenant members CRUD API and role management`

### Task 3: Tenant Members Management UI in Backoffice Web (Issue #18)
- Update `apps/backoffice-web/src/app/(dashboard)/tenants/ClientPage.tsx` & `actions.ts`:
  - Add Members section with member list table, role badges, invite modal, role select, and remove action.
- Commit message: `feat(web): implement members management UI, invite modal, and role selector`

### Task 4: SaaS Plan Quota Guard & Engine (Issue #14)
- Create `apps/control-plane-api/src/saas/saas.module.ts`, `saas.service.ts`, `quota.guard.ts`.
- Enforce service count and deployment count limits per tenant plan (`free`, `pro`, `enterprise`).
- Return HTTP 402 with upgrade callout when limits are exceeded.
- Verify build (`npx turbo run build`).
- Commit message: `feat(saas): implement plan quota guard, usage tracking, and SaaS engine`
