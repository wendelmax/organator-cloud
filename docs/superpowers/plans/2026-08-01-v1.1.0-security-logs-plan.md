# Implementation Plan: Milestone v1.1.0 (Security & Real-Time Logs)

Branch: `feat/v1.1.0-security-and-logs`

## Tasks

### Task 1: AES-256-GCM Encryption Utility & Secret Storage (Issue #13)
- Create `packages/cloud-providers/src/crypto.ts` with `encryptSecret` and `decryptSecret`.
- Export crypto utility and ensure cloud provider clients (AWS, Vercel, VPS SSH) decrypt credentials safely before API/SSH execution.
- Create `apps/control-plane-api/src/common/crypto.util.ts`.
- Commit message: `sec(crypto): implement AES-256-GCM encryption for cloud credentials and SSH keys`

### Task 2: Real-Time Deployment Log Streaming via SSE & Redis Pub/Sub (Issue #12)
- Update `apps/provisioner-worker/src/index.ts` to publish log events to Redis Pub/Sub channel `deploy_logs:<id>`.
- Add `GET /v1/services/deployments/:id/stream` in `apps/control-plane-api/src/services/services.controller.ts` returning SSE stream via RxJS Observable.
- Update `apps/backoffice-web/src/app/(dashboard)/services/[id]/ClientPage.tsx` to use `EventSource` for real-time log streaming in the terminal UI.
- Verify build (`npx turbo run build`).
- Commit message: `feat(logs): implement real-time deployment log streaming via Redis Pub/Sub and SSE`
